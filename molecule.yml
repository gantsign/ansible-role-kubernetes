---
docker:
  containers:

      - name: ansible-role-kubernetes-01
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1

      - name: ansible-role-kubernetes-02
        image: ubuntu
        image_version: latest
        ansible_groups:
          - group1