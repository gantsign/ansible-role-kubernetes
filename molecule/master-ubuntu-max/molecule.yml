---
dependency:
  name: galaxy

driver:
  name: docker

lint:
  name: yamllint

platforms:
  - name: ansible-role-kubernetes-ubuntu-max
    image: ubuntu:18.04

provisioner:
  name: ansible
  playbooks:
    converge: ../default/playbook.yml
  lint:
    name: ansible-lint

scenario:
  name: master-ubuntu-max

verifier:
  name: testinfra
  directory: ../default/tests
  lint:
    name: flake8
